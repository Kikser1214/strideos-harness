# Submission image set

The gallery must make the shipped plugin clear before showing the optional reference implementation. Capture these two plugin-first images before final submission:

- `plugin-six-skills.png` — **One installable plugin, six focused skills.** Show `codex plugin list` with the `strideos@strideos` marketplace entry or the plugin manifest beside the exact `coach-athlete`, `plan-training`, `use-training-data`, `support-fueling`, `schedule-coaching`, and `build-coach-room` inventory.
- `coach-room-review.png` — **Bring a trusted human into the Training Circle.** Show a reviewer comment attached to an exact workout, the proposed before/after diff, and athlete-only approval. Label demo-only identity or persistence honestly.

Recommended Devpost order after those captures:

1. `plugin-six-skills.png` — shipped product and installable package.
2. `coach-room-review.png` — central Training Circle collaboration feature.
3. `dashboard-hero.png` — **Legacy reference screenshot.** Keep it out of current README, landing, and submission assets because its old headline predates the plugin-first product framing.
4. `first-run-desktop.png` — **The athlete map starts before the plan.** Eleven steps cover baseline, safety, goals, strength, schedule, evidence routes, coaching style, optional fuel, and delivery.
5. `training-plan.png` — **A four-week plan with reasons and a brake.** Running, technique-first strength, recovery, availability, and the reduced fourth week are visible before approval.
6. `fuel-companion.png` — **Fuel guidance obeys the athlete’s number and privacy choices.** Meal photos remain optional estimates and do not prove allergen safety.
7. `first-run-mobile.png` — **The optional reference app is responsive.** The primary action stays reachable at a 390 px viewport.

Use `docs/ARCHITECTURE.md` as the architecture slide or capture its rendered Mermaid diagram from the public GitHub page. Do not crop out mode labels that distinguish personal local demo, synthetic judge data, or live GPT-5.6.
